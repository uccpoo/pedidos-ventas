server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/tabla_pedidosventas
    username: root
    password: Pipeman06.
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
